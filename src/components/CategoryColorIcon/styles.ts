import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    icon: {
        height: 15,
        width: 15,
        borderRadius: 20,
        marginRight: 8,
    },
});
export default styles;
